# AI-Finance-Controller
Multi-source reconciliation agent that matches a bank feed against a GL ledger through a 4-stage cascade, reporting a measured match rate (verified against a hidden ground-truth key) instead of a self-reported one.
